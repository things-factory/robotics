import { expect } from 'chai'
import '../../server/graphql'

describe('CameraStreamer', function () {
  before(function () {})

  after(function () {})

  describe('listen', function () {
    it('should connectable from websocket client', done => {})
  })
})
